const moneda = new Intl.NumberFormat('es-PE', { style: 'currency', currency: 'PEN' });
function ProductoList({ productos, cargando, onEditar, onEliminar }) {
  if (cargando) return <section class="mt-7 rounded-2xl bg-white px-6 py-12 text-center text-slate-500 shadow-xl ring-1 ring-slate-200">Cargando productos...</section>;
  return <section class="mt-7 overflow-hidden rounded-2xl bg-white shadow-xl shadow-slate-200/70 ring-1 ring-slate-200"><div class="flex items-center justify-between border-b border-slate-100 px-6 py-5 sm:px-8"><h2 class="text-xl font-semibold text-slate-800">Productos</h2><span class="rounded-full bg-indigo-50 px-3 py-1 text-sm font-semibold text-indigo-700">{productos.length}</span></div>{productos.length === 0 ? <p class="px-6 py-12 text-center text-slate-500">Aún no hay productos. Crea el primero arriba.</p> : <ul class="divide-y divide-slate-100">{productos.map((producto) => <li key={producto.id} class="flex flex-col gap-4 px-6 py-5 sm:flex-row sm:items-center sm:justify-between sm:px-8"><div><p class="font-semibold text-slate-800">{producto.nombre}</p><p class="mt-1 text-sm text-slate-500">{moneda.format(Number(producto.precio))}</p>{producto.usuario_nombre && <p class="mt-1 text-xs text-slate-400">Propietario: {producto.usuario_nombre} ({producto.usuario_email})</p>}</div><div class="flex gap-2"><button type="button" onClick={() => onEditar(producto)} class="rounded-lg border border-slate-300 px-3 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50">Editar</button><button type="button" onClick={() => onEliminar(producto)} class="rounded-lg bg-rose-50 px-3 py-2 text-sm font-semibold text-rose-700 hover:bg-rose-100">Eliminar</button></div></li>)}</ul>}</section>;
}
export default ProductoList;
